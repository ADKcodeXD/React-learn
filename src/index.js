import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './08_组件部分知识点/App3';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render (
    <React.StrictMode>
        <App/>
    </React.StrictMode>
);
