import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './07_setState使用/App2';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
